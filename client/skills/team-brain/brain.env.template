# team-brain client config — provisioned by the Podclave org overlay.
# IDENTICAL on every VM. Identity is NOT set here: brain.sh reads the teammate's
# email from ~/.podclave/user-email (falls back to git email / $USER).
# Fill BRAIN_URL + BRAIN_SECRET from the server install output.
BRAIN_URL="https://YOUR-BRAIN.sprites.app"
BRAIN_SECRET="REPLACE_WITH_TEAM_SECRET"
BRAIN_USER=""
